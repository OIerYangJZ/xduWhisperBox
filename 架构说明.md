**校园综合服务微信小程序项目架构说明**

面向“xdu校园小程序”的技术架构、模块划分与实施边界

文档目的：基于《校园综合服务微信小程序项目要点清单》整理项目架构方案，明确小程序、现有 App/Web 项目、共享 NestJS 后端、CMS 管理端、AI/RAG 服务、数据与安全治理之间的关系。

核心约束：已有完善的相似 App \+ Web 项目，本项目应优先复用一套后端服务、数据库模型、认证体系、CMS 管理能力和运维体系，新增微信小程序端适配层与少量微信生态集成能力，避免重复建设。

# **1\. 架构目标与设计原则**

* 统一后端：App、Web、小程序共用同一套 NestJS 业务服务、MySQL 主库和管理员 CMS，业务规则集中在服务端，避免多端逻辑分叉。  
* 端侧轻量：微信小程序主要承担展示、交互、微信登录授权、文件上传和订阅消息触发，不在端侧沉淀复杂业务规则。  
* 模块化演进：校园新闻、学院专区、树洞社区、AI 问答、身份认证、用户中心按领域模块拆分，便于和现有 App/Web 模块映射复用。  
* 安全合规优先：匿名社区、评论回复、AI 问答和管理员操作均纳入审核、日志、风控和权限体系。  
* MVP 先行：第一版聚焦校园新闻、树洞、AI 校园问答、登录认证/个人信息；私信、自动课表、实时聊天、二手交易和复杂 AI 功能进入二期。

# **2\. 总体架构**

采用“多端统一业务后端 \+ CMS 管理端 \+ AI/RAG 辅助服务 \+ 基础设施中台”的分层架构。小程序不是新建独立后端，而是作为现有平台的一个新客户端接入。

架构关系如下：

微信小程序 / App / Web 前端  →  API 网关或 BFF 适配层  →  NestJS 业务服务  →  MySQL / Redis / 对象存储 / 搜索索引 / 向量库 ；管理员通过 Web CMS 访问同一后端进行内容发布、审核、用户封禁、数据统计和日志审计。

| 层级 | 组成 | 职责 | 复用策略 |
| :---- | :---- | :---- | :---- |
| 客户端层 | 微信小程序、现有 App、现有 Web | 提供校园、AI、我的三大入口；承担 UI、路由、登录授权、表单校验和本地缓存 | 新增小程序端；App/Web 保持原有能力，统一调用后端 API |
| 接入层 | API 网关、BFF、微信登录适配器 | 统一鉴权、限流、版本控制、微信 code2Session、订阅消息回调 | 优先复用现有 API；仅对微信生态差异增加适配接口 |
| 业务服务层 | NestJS 模块化服务 | 用户、内容、社区、评论、收藏、举报、审核、AI、学院、日志等领域逻辑 | 作为唯一业务规则中心，供小程序/App/Web/CMS 共用 |
| 管理端层 | 现有 CMS / 管理员 Web | 新闻发布、学院内容维护、帖子审核、举报处理、封号、操作日志、数据统计 | 直接复用现有管理员系统，补充小程序所需字段和审核流程 |
| 数据与基础设施层 | MySQL、Redis、对象存储、搜索、向量库、日志系统 | 持久化、缓存、图片存储、全文检索、RAG 检索、审计追踪 | 在现有基础设施上扩展表结构、索引和任务队列 |
| 外部服务层 | 微信开放能力、第三方 AI API、学校官网/教务资料源 | 微信认证、模板/订阅消息、AI 问答、知识库采集 | 采用适配器模式隔离外部服务变化 |

# **3\. 前端架构设计**

## **3.1 微信小程序端技术选型**

结合已有 App \+ Web 项目，前端选型不应只看小程序本身，而要看与现有代码体系的复用程度。

| 方案 | 适用条件 | 优点 | 风险/注意事项 |
| :---- | :---- | :---- | :---- |
| 微信小程序原生 | 团队熟悉小程序原生；项目只面向微信生态 | 性能稳定、生态兼容性最好、审核风险低 | 与现有 Web/App 代码复用较少，需要单独维护端侧组件 |
| Taro | 现有前端偏 React 技术栈，希望复用部分组件和工程习惯 | 多端能力较好，适合 React 团队，工程化成熟 | 复杂组件和微信特性仍需适配，不能盲目追求全量复用 |
| uni-app | 现有 App 或 H5 偏 Vue 技术栈，希望多端统一 | Vue 生态友好，多端产物覆盖广 | 大型项目需规范工程边界，避免平台差异污染业务代码 |

推荐策略：如果现有 App/Web 是 React 技术栈，优先 Taro；如果是 Vue/uni 技术栈，优先 uni-app；如果现有项目复用价值主要在后端而非前端，则使用微信小程序原生更稳。无论选型如何，都应通过 API SDK、类型定义、接口约定和设计规范复用，而不是强行复用所有 UI 代码。

## **3.2 小程序页面结构**

| Tab | 核心页面 | 对应后端模块 | MVP 边界 |
| :---- | :---- | :---- | :---- |
| 校园 | 校园新闻、学院专区、树洞列表、帖子详情、搜索/筛选 | content、college、community、comment、favorite、report | 保留新闻、学院内容、树洞发帖/评论/点赞/举报/收藏 |
| AI | AI 校园助手、会话列表、问答详情 | ai、rag、knowledge-base、conversation | 初期只做 RAG 问答、总结和信息检索，不做复杂智能规划 |
| 我的 | 个人资料、身份认证、收藏、设置、反馈 | user、auth、student-verification、notification | 完成微信绑定校园卡、个人信息展示、基础设置 |

# **4\. 后端架构设计**

后端以 NestJS 为核心，保持领域模块化。小程序与 App/Web 共享领域服务，差异点通过 controller、DTO、guard、adapter 或 BFF 处理。

| NestJS 模块 | 主要能力 | 关键实体/接口 |
| :---- | :---- | :---- |
| AuthModule | 微信登录、JWT/Session、权限校验、校园卡绑定状态校验 | WxLogin、UserToken、StudentBinding、RoleGuard |
| UserModule | 用户资料、学院年级、头像昵称、封禁状态 | User、StudentProfile、UserStatus |
| ContentModule | 学校通知、学院公告、活动比赛、讲座信息发布与检索 | Article、Category、Tag、Like、Favorite |
| CollegeModule | 学院专区、学院管理员权限、学院内容隔离 | College、Major、CollegeAdminScope |
| CommunityModule | 树洞帖子、匿名/实名发帖、图片、标签、热榜 | Post、PostImage、PostTag、AnonymousProfile |
| CommentModule | 评论、回复、点赞、通知触发 | Comment、Reply、CommentLike、NotificationEvent |
| ModerationModule | 敏感词、审核队列、举报、封禁、内容状态机 | SensitiveWord、ReviewTask、Report、BanRecord |
| AIModule | AI 问答、RAG 检索、会话、调用配额 | Conversation、Message、KnowledgeChunk、AIUsage |
| AdminModule | CMS 权限、后台操作、统计报表 | AdminUser、Role、Permission、OperationLog |
| LogModule | 管理员日志、用户行为日志、接口审计 | OperationLog、AuditLog、RequestLog |

## **4.1 API 复用与小程序适配**

* 通用 API：内容列表、帖子详情、评论、点赞、收藏、举报、AI 问答、用户资料等接口应与 App/Web 共用。  
* 小程序适配 API：微信登录、手机号/校园卡绑定、订阅消息授权、图片上传凭证、小程序分享参数解析等接口单独封装。  
* 接口版本：建议使用 /api/v1 作为稳定版本，小程序端通过 clientType=wechat-miniprogram 或独立 Header 标识来源。  
* 权限中间件：未登录用户可浏览公开内容；完成微信登录但未校园认证只能使用部分功能；完成学生认证后开放发帖、评论、AI 等核心能力。

# **5\. 管理员 CMS 架构**

管理员系统继续采用现有 Web CMS，不为小程序单独开发后台。需要在原 CMS 中补充小程序相关菜单、字段、审核流程和日志维度。

| 后台菜单 | 功能 | 角色范围 |
| :---- | :---- | :---- |
| 校园新闻管理 | 发布学校通知、活动、讲座、比赛，支持图文、标签、置顶、搜索 | 超级管理员、内容管理员 |
| 学院专区管理 | 按学院发布通知、资料、经验帖，配置学院标签和分类 | 学院管理员、超级管理员 |
| 树洞审核中心 | 敏感内容审核、举报处理、帖子下架、评论删除、用户封禁 | 内容审核员、超级管理员 |
| AI 知识库管理 | 导入官网/FAQ/学院资料，管理知识条目、分片、索引状态 | AI 管理员、超级管理员 |
| 用户与认证管理 | 查看学生认证状态、封禁/解封、异常账号处理 | 超级管理员 |
| 日志与统计 | 管理员操作日志、内容增长、举报量、AI 调用量、活跃用户 | 超级管理员 |

日志要求：所有后台写操作必须记录 adminId、角色、操作对象、操作类型、前后状态摘要、IP、User-Agent、时间戳和结果。对删除、封禁、审核驳回等高风险操作建议支持二次确认或备注。

# **6\. 数据架构与核心表设计**

数据层优先扩展现有 MySQL 模型。若现有 App/Web 已有用户、内容、评论、收藏、日志表，应通过字段补充和关联表扩展，而不是另建小程序孤岛库。

| 领域 | 建议表/集合 | 说明 |
| :---- | :---- | :---- |
| 用户认证 | users、student\_profiles、wechat\_bindings、user\_roles | 统一用户主表；微信 openid/unionid 与平台用户绑定；校园卡认证信息单独存储 |
| 内容发布 | articles、article\_categories、article\_tags、article\_stats | 校园新闻、学院公告、活动比赛、讲座信息可复用统一内容模型 |
| 学院专区 | colleges、college\_resources、college\_admin\_scopes | 学院内容按 collegeId 隔离，学院管理员只管理授权范围 |
| 树洞社区 | posts、post\_images、post\_tags、post\_stats、anonymous\_profiles | 帖子支持匿名/实名状态；匿名身份在单帖或会话维度生成，不暴露真实用户 |
| 互动行为 | comments、comment\_replies、likes、favorites、reports | 评论回复通知可作为一期替代私信系统 |
| 审核风控 | sensitive\_words、review\_tasks、moderation\_records、ban\_records | 敏感词命中进入审核队列，举报触发人工审核和封禁流程 |
| AI/RAG | knowledge\_sources、knowledge\_chunks、vector\_index\_jobs、ai\_conversations、ai\_messages、ai\_usage\_logs | 知识源、分片、向量索引、会话消息和调用成本分开管理 |
| 审计日志 | admin\_operation\_logs、audit\_logs、request\_logs | 支撑后台追责、异常排查和合规审计 |

# **7\. AI/RAG 模块架构**

AI 模块初期采用 RAG 模式，不自建大模型。系统将学校官网、教务通知、FAQ、地图、学院资料等内容沉淀为知识库，通过检索增强生成答案。

推荐流程：资料采集/人工导入 → 文本清洗 → 分片 → 向量化 → 入库 → 用户提问 → 查询改写 → 向量检索/关键词检索 → 上下文重排 → 调用第三方 AI API → 返回答案与引用来源。

* 知识库管理必须接入 CMS，支持知识源上下线、重建索引、失效标记和人工校正。  
* AI 答案应标注“仅供参考”，对奖学金、考试、政策等问题优先给出处或链接。  
* 需要设置用户级、IP 级、全局级调用频率限制，并记录 token/费用消耗。  
* 未来可扩展 AI 课表分析、选课建议、考试提醒、校园导航，但这些不进入 MVP。

# **8\. 权限、安全与合规设计**

| 对象 | 控制策略 |
| :---- | :---- |
| 学生用户 | 微信登录 \+ 校园卡绑定；认证前限制发帖/评论/AI 使用；封禁用户禁止互动 |
| 学院管理员 | RBAC \+ 数据范围控制，只能管理本学院内容和帖子 |
| 超级管理员 | 拥有用户、内容、审核、统计、封禁和系统配置权限；高风险操作记录日志 |
| 匿名发帖 | 前端展示匿名，服务端仍保留真实 userId 映射，用于举报、风控和合规追责 |
| 内容安全 | 敏感词过滤、图片审核、举报系统、人工审核、黑名单和封号机制组合使用 |
| 接口安全 | JWT/Session、HTTPS、签名或 CSRF 防护、限流、防刷、上传类型/大小校验 |
| 数据安全 | 校园卡号等敏感字段脱敏展示，加密或哈希存储关键绑定标识，最小权限访问 |

# **9\. 部署与运维架构**

* 环境划分：dev、test/staging、prod 三套环境；小程序体验版绑定 staging，正式版绑定 prod。  
* 部署方式：NestJS 服务容器化部署，配合 Nginx/API Gateway、PM2 或 Kubernetes；CMS 静态资源通过 CDN 或对象存储分发。  
* 基础组件：MySQL 主库、Redis 缓存、对象存储、消息队列、全文搜索/向量检索服务、日志采集与监控告警。  
* 可观测性：记录接口耗时、错误率、AI 调用耗时、审核积压量、举报处理时长、登录成功率和上传失败率。  
* 备份策略：MySQL 定时备份；对象存储开启版本/生命周期策略；知识库索引可重建但源数据必须保留。

# **10\. MVP 范围与迭代路线**

| 阶段 | 范围 | 交付重点 |
| :---- | :---- | :---- |
| MVP 第一版 | 校园新闻、学院专区基础浏览、树洞发帖/评论/点赞/收藏/举报、AI RAG 问答、微信登录与校园卡绑定、个人信息 | 验证校园超级入口价值，打通小程序与现有后端/CMS 的闭环 |
| 1.1 增强版 | 内容搜索、标签筛选、热榜、评论回复通知、后台统计、审核效率优化 | 提升内容分发和社区活跃度 |
| 二期 | 私信、自动课表、二手交易、AI 课表分析、选课建议、考试提醒、校园导航 | 在用户规模、审核能力和数据接入条件成熟后逐步上线 |

# **11\. 关键风险与工程建议**

* 不要为小程序复制一套后端。复制后端会导致用户、内容、审核、日志和权限体系长期分裂，维护成本快速上升。  
* 匿名社区必须先做审核和封禁闭环，再扩大流量。树洞类功能的主要风险不是技术实现，而是内容治理。  
* AI 问答必须以知识库质量为先。RAG 的效果高度依赖资料更新、分片策略、检索质量和答案引用机制。  
* 校园卡绑定涉及敏感信息，建议只保存必要的认证结果和脱敏标识，避免过度采集。  
* 前端多端复用应以接口、类型、设计规范和业务组件为主，避免被平台差异拖累。

# **12\. 推荐落地顺序**

| 顺序 | 任务 | 产出 |
| :---- | :---- | :---- |
| 1 | 梳理现有 App/Web 后端模块与数据表 | 复用清单、需扩展字段、接口差异表 |
| 2 | 确定小程序技术栈与页面原型 | Tab Bar、页面路由、核心交互流程 |
| 3 | 实现微信登录与校园卡绑定适配 | 统一用户身份、认证状态、权限守卫 |
| 4 | 接入内容与社区基础 API | 新闻、学院专区、树洞、评论、点赞、收藏、举报 |
| 5 | 改造 CMS 审核与日志能力 | 内容发布、审核队列、封禁、操作日志 |
| 6 | 建设 AI/RAG 最小闭环 | 知识库导入、检索、问答、调用日志、限流 |
| 7 | 联调、压测、安全审查与小程序提审 | 体验版验收、线上监控、发布回滚方案 |

# **13\. 结论**

本项目最合理的架构是在已有 App \+ Web 项目基础上增加微信小程序客户端，并复用统一 NestJS 后端、MySQL 数据模型、CMS 管理系统、审核风控和日志体系。小程序端聚焦校园入口体验，后端聚焦统一业务能力，CMS 聚焦内容和治理闭环，AI 模块以 RAG 方式渐进接入。按照该架构推进，可以在较低重复建设成本下完成 MVP，并为后续私信、课表、交易和复杂 AI 能力预留扩展空间。